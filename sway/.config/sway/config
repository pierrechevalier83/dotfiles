# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Startup application
#
exec --no-startup-id workstyle &> /tmp/workstyle.log
exec --no-startup-id autotiling-rs
exec --no-startup-id ~/.cargo/bin/syngestures
exec --no-startup-id ydotoold
exec --no-startup-id dunst

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# Your preferred terminal emulator
set $term "alacritty"
set $browser "firefox"
# Your preferred screenlocker
set $lock "swaylock -i ~/Pictures/wallpaper.jpg"
# Your preferred application launcher
# Note: it's recommended that you pass the final command to sway
set $menu FZF_DEFAULT_OPTS='--height 100% --reverse --border' alacritty -t menu -e bash -c 'dmenu_path | fzf | xargs -r swaymsg -t command exec', for_window [title="menu"] border pixel 0, for_window [title="menu"] floating enable, for_window [title=menu] move absolute position 0 30

for_window [app_id="gthumb"] floating enable
for_window [app_id="gnome-calculator"] floating enable

default_border pixel 1
smart_borders on

set $theme_alpha DD
set $inactive_alpha AA

set $bg_neutral "#002b36$theme_alpha"
set $bg_active1 "#073642$theme_alpha"
set $bg_active2 "#002b36$inactive_alpha"
set $text_color "#eee8d5"
set $bg_urgent "#eee8d5$theme_alpha"
set $text_color_urgent "#073642$theme_alpha"
set $border_focused "#268bd2$theme_alpha"

# border, background, text, indicator, child border
client.unfocused $bg_active2 $bg_active2 "$text_color$inactive_alpha" $bg_neutral $bg_active2
client.focused $border_focused $bg_neutral $text_color $bg_neutral $border_focused
client.focused_inactive $bg_neutral $bg_neutral $text_color $bg_neutral $bg_active1
client.urgent $bg_urgent $bg_urgent $text_color_urgent $bg_neutral $bg_urgent


### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
output * bg ~/Pictures/wallpaper.jpg fill

set $laptop eDP-1

output $laptop resolution 1920x1080 position 0,0
bindswitch lid:on output $laptop disable
bindswitch lid:off output $laptop enable

set $samsung DP-5
output $samsung resolution 2560x1440 position 1920,-360

#
# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

### Idle configuration
#
# Example configuration:
#
#exec swayidle -w \
#         timeout 300 'swaylock -f -c 000000' \
#         timeout 600 'swaymsg "output * dpms off"' \
#              resume 'swaymsg "output * dpms on"' \
#         before-sleep 'swaylock -f -c 000000'
#
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

### Input configuration
#
# Example configuration:
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.
input "2:7:SynPS/2_Synaptics_TouchPad" {
	tap enabled
	click_method button_areas
}

input type:keyboard {
    xkb_options caps:super
}

### Key bindings
#
# Basics:
#
    # Start a terminal
    bindsym $mod+Return exec $term

    # Kill focused window
    bindsym $mod+q kill

    # Exit sway
    bindsym $mod+Shift+q exec "swaymsg exit"

    # Start your launcher
    bindsym $mod+p exec $menu

    # Lock your screen
	bindsym $mod+l exec $lock

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Reload the configuration file
    bindsym $mod+Shift+c reload


    # Change screen brightness
    bindsym XF86MonBrightnessUp         exec brightnessctl s +5%
    bindsym XF86MonBrightnessDown       exec brightnessctl s 5%-

    # Change keyboard brightness
	bindsym XF86MonBrightnessUp+Shift   exec brightnessctl -d *::kbd_backlight set 10%+
	bindsym XF86MonBrightnessDown+Shift exec brightnessctl -d *::kbd_backlight set 10%-

    # Sound (output)
	bindsym XF86AudioRaiseVolume        exec pactl set-sink-volume @DEFAULT_SINK@ +5%
	bindsym XF86AudioLowerVolume        exec pactl set-sink-volume @DEFAULT_SINK@ -5%
	bindsym XF86AudioMute               exec pactl set-sink-mute @DEFAULT_SINK@ toggle
    # Mic
	bindsym XF86AudioRaiseVolume+Shift  exec pactl set-source-volume @DEFAULT_SOURCE@ +5%
	bindsym XF86AudioLowerVolume+Shift  exec pactl set-source-volume @DEFAULT_SOURCE@ -5%
	bindsym XF86AudioMute+Shift         exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
	bindsym XF86AudioMicMute            exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
#
# Moving around:
#
    # Move your focus around
#    bindsym $mod+$left focus left
#    bindsym $mod+$down focus down
#    bindsym $mod+$up focus up
#    bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    bindsym alt+Left focus left
    bindsym alt+Down focus down
    bindsym alt+Up focus up
    bindsym alt+Right focus right

    # Move the focused window with the same, but add Shift
#    bindsym $mod+Shift+$left move left
#    bindsym $mod+Shift+$down move down
#    bindsym $mod+Shift+$up move up
#    bindsym $mod+Shift+$right move right
    # Ditto, with arrow keys
    bindsym alt+Shift+Left move left
    bindsym alt+Shift+Down move down
    bindsym alt+Shift+Up move up
    bindsym alt+Shift+Right move right
#
# Workspaces:
#
	# Start with workspace 1
	workspace 1
	workspace +
    # Switch to workspace
    bindsym $mod+1 workspace number 1
    bindsym $mod+2 workspace number 2
    bindsym $mod+3 workspace number 3
    bindsym $mod+4 workspace number 4
    bindsym $mod+5 workspace number 5
    bindsym $mod+6 workspace number 6
    bindsym $mod+7 workspace number 7
    bindsym $mod+8 workspace number 8
    bindsym $mod+9 workspace number 9	
    bindsym $mod+0 workspace number 0
    bindsym ctrl+alt+1 workspace number 1
    bindsym ctrl+alt+2 workspace number 2
    bindsym ctrl+alt+3 workspace number 3
    bindsym ctrl+alt+4 workspace number 4
    bindsym ctrl+alt+5 workspace number 5
    bindsym ctrl+alt+6 workspace number 6
    bindsym ctrl+alt+7 workspace number 7
    bindsym ctrl+alt+8 workspace number 8
    bindsym ctrl+alt+9 workspace number 9
    bindsym ctrl+alt+0 workspace number 0
    bindsym ctrl+alt+left workspace prev
    bindsym ctrl+alt+right workspace next
    bindsym ctrl+alt+up workspace prev
    bindsym ctrl+alt+down workspace next
    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace number 1; workspace number 1
    bindsym $mod+Shift+2 move container to workspace number 2; workspace number 2
    bindsym $mod+Shift+3 move container to workspace number 3; workspace number 3
    bindsym $mod+Shift+4 move container to workspace number 4; workspace number 4
    bindsym $mod+Shift+5 move container to workspace number 5; workspace number 5
    bindsym $mod+Shift+6 move container to workspace number 6; workspace number 6
    bindsym $mod+Shift+7 move container to workspace number 7; workspace number 7
    bindsym $mod+Shift+8 move container to workspace number 8; workspace number 8
    bindsym $mod+Shift+9 move container to workspace number 9; workspace number 9
    bindsym $mod+Shift+0 move container to workspace number 0; workspace number 0
    bindsym ctrl+alt+Shift+1 move container to workspace number 1; workspace number 1
    bindsym ctrl+alt+Shift+2 move container to workspace number 2; workspace number 2
    bindsym ctrl+alt+Shift+3 move container to workspace number 3; workspace number 3
    bindsym ctrl+alt+Shift+4 move container to workspace number 4; workspace number 4
    bindsym ctrl+alt+Shift+5 move container to workspace number 5; workspace number 5
    bindsym ctrl+alt+Shift+6 move container to workspace number 6; workspace number 6
    bindsym ctrl+alt+Shift+7 move container to workspace number 7; workspace number 7
    bindsym ctrl+alt+Shift+8 move container to workspace number 8; workspace number 8
    bindsym ctrl+alt+Shift+9 move container to workspace number 9; workspace number 9
    bindsym ctrl+alt+Shift+0 move container to workspace number 0; workspace number 0
    bindsym ctrl+alt+Shift+up move container to workspace prev; workspace prev
    bindsym ctrl+alt+Shift+down move container to workspace next; workspace next
    bindsym ctrl+alt+Shift+left move container to workspace prev; workspace prev
    bindsym ctrl+alt+Shift+right move container to workspace next; workspace next
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+h splith
    bindsym $mod+v splitv
    bindsym $mod+tab split toggle

    # Switch the current container between different layout styles
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split
    bindsym $mod+a layout toggle all

    # Make the current focus fullscreen
    bindsym $mod+Up fullscreen
    
	bindsym $mod+f exec $browser

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+u focus parent
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+minus scratchpad show
#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Modal navigation

mode "window manager" {
    bindsym Left focus left
    bindsym Down focus down
    bindsym Up focus up
    bindsym Right focus right
    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+n mode "window manager"

#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.
#set $bg_neutral "#002b36$theme_alpha"
#set $bg_active1 "#073642$theme_alpha"
#set $bg_active2 "#002b36$inactive_alpha"
#set $text_color "#eee8d5"
#set $bg_urgent "#eee8d5$theme_alpha"
#set $text_color_urgent "#073642$theme_alpha"
#set $border_focused "#268bd2$theme_alpha"
set $base03    #002b36
set $base02    #073642
set $base01    #586e75
set $base00    #657b83
set $base0     #839496
set $base1     #93a1a1
set $base2     #eee8d5
set $base3     #fdf6e3
set $yellow    #b58900
set $orange    #cb4b16
set $red       #dc322f
set $magenta   #d33682
set $violet    #6c71c4
set $blue      #268bd2
set $cyan      #2aa198
set $green     #859900

bar {
    font Hack Mono, FontAwesome 12
    position top
    status_command i3status-rs
    colors {
        separator $border_focused
        background $bg_neutral
        statusline #dddddd
		#                 border          background      text
        focused_workspace $blue$theme_alpha $blue$theme_alpha $text_color
        active_workspace $blue$theme_alpha $bg_active1 $text_color
        inactive_workspace $bg_neutral $bg_neutral $text_color
        urgent_workspace $orange$theme_alpha $orange$theme_alpha $text_color
    }
}
#bar {
#    swaybar_command waybar
#}

#
# Notification Area:
#
# nag
set {
  $nag         exec swaynagmode --mode "nag" --mode-default "default"
  $nag_exit    exec swaynagmode --exit
  $nag_confirm exec swaynagmode --confirm
  $nag_select  exec swaynagmode --select
}
mode "nag" {
  bindsym {
    Ctrl+d    mode "default"

    Ctrl+c    $nag_exit
    q         $nag_exit
    Escape    $nag_exit

    Return    $nag_confirm

    Tab       $nag_select prev
    Shift+Tab $nag_select next

    Left      $nag_select next
    Right     $nag_select prev

    Up        $nag_select next
    Down      $nag_select prev
  }
}
bindsym {
  # Exit sway (logs you out of your Wayland session)
  $mod+Shift+e $nag -t "warning" -m "Exit Sway?" -b "Exit" "swaymsg exit" -b "Reload" "swaymsg reload"
  $mod+Shift+Equal $nag -t "warning" -m "New workspace" -b "" "swaymsg workspace 1" -b "" "swaymsg workspace 2" -b "" "swaymsg workspace 3" -b "" "swaymsg workspace 4" -b "" "swaymsg workspace 5"
}
# -R is recommended for swaynag_command so that, upon a syntax error in your sway config, the
# 'Reload Sway' option will be initially selected instead of the 'Exit Sway' option
swaynag_command $nag --mode "nag" --mode-default "default" -R

include /etc/sway/config.d/*
